import "reflect-metadata";
import { container } from "tsyringe";


export class readmeGenerator {
  public connection: any;
  public utilities: any;
  public instance: any;

  constructor() {}

  initialize = async () => {
    
  };

  public addFile(data: any) {
     //add readme to cwd
  }

  public addContent() {
    //add content to generate readme 
  }
}